'use client';
import {useState} from 'react';
import {createClient} from '@supabase/supabase-js';
function sb(){const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;return u&&k?createClient(u,k):null}
export default function Auth(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[signup,setSignup]=useState(false),[msg,setMsg]=useState('');async function go(){const s=sb();if(!s)return setMsg('Supabase environment variables are not configured.');const r=signup?await s.auth.signUp({email,password}):await s.auth.signInWithPassword({email,password});setMsg(r.error?.message||(signup?'Check your email to confirm your account.':'Signed in successfully.'))}return <main style={{maxWidth:460,margin:'80px auto',padding:24}}><a href="/">← SHOKON</a><h1>{signup?'Create account':'Welcome back'}</h1><p className="muted">{signup?'Create your SHOKON account.':'Sign in to book and message people.'}</p><input style={{width:'100%',padding:14,margin:'8px 0',background:'#202220',color:'#fff',border:'1px solid #333',borderRadius:12}} type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input style={{width:'100%',padding:14,margin:'8px 0',background:'#202220',color:'#fff',border:'1px solid #333',borderRadius:12}} type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="primary" style={{height:50,width:'100%',marginTop:8}} onClick={go}>{signup?'Sign up':'Sign in'}</button>{msg&&<p>{msg}</p>}<button className="navbtn" style={{width:'100%',marginTop:12}} onClick={()=>{setSignup(!signup);setMsg('')}}>{signup?'Already have an account? Sign in':'Need an account? Sign up'}</button></main>}
