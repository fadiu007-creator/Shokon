import { useState } from 'react';
import { router } from 'expo-router';
import { Pressable, StyleSheet, Text, TextInput, View } from 'react-native';
import { requireSupabase } from '../lib/supabase';

export default function Auth(){const [signup,setSignup]=useState(false),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');async function submit(){try{const s=requireSupabase();const r=signup?await s.auth.signUp({email,password}):await s.auth.signInWithPassword({email,password});if(r.error)throw r.error;setMsg(signup?'Check your email if confirmation is enabled.':'Signed in.');if(!signup)router.replace('/')}catch(e:any){setMsg(e.message)}}return <View style={s.page}><Text style={s.logo}>SHOKON.</Text><Text style={s.title}>{signup?'Create account':'Welcome back'}</Text><TextInput style={s.input} autoCapitalize="none" keyboardType="email-address" placeholder="Email" value={email} onChangeText={setEmail}/><TextInput style={s.input} secureTextEntry placeholder="Password" value={password} onChangeText={setPassword}/>{msg?<Text style={s.msg}>{msg}</Text>:null}<Pressable style={s.primary} onPress={submit}><Text style={s.primaryText}>{signup?'Sign up':'Sign in'}</Text></Pressable><Pressable style={s.outline} onPress={()=>setSignup(!signup)}><Text>{signup?'Sign in instead':'Create an account'}</Text></Pressable><Pressable onPress={()=>router.back()}><Text style={{textAlign:'center',marginTop:18}}>Back</Text></Pressable></View>}
const s=StyleSheet.create({page:{flex:1,justifyContent:'center',padding:24,backgroundColor:'#f7f7f5'},logo:{fontSize:25,fontWeight:'900',textAlign:'center'},title:{fontSize:28,fontWeight:'800',textAlign:'center',marginVertical:28},input:{backgroundColor:'#fff',borderWidth:1,borderColor:'#ddd',borderRadius:12,padding:14,marginVertical:6},primary:{backgroundColor:'#111',padding:15,borderRadius:12,marginTop:10},primaryText:{color:'#fff',textAlign:'center',fontWeight:'800'},outline:{borderWidth:1,borderColor:'#222',padding:14,borderRadius:12,marginTop:8,alignItems:'center'},msg:{color:'#555',marginVertical:8,textAlign:'center'}});
